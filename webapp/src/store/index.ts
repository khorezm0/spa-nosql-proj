import Vue from 'vue'
import Vuex from 'vuex'
import Element from 'element-ui'

Vue.use(Element);
Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
});
